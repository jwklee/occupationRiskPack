% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalk.R
\name{crosswalk_isco_soc}
\alias{crosswalk_isco_soc}
\title{Crosswalk between ISCO08 and SOC2010}
\usage{
crosswalk_isco_soc(
  data = NULL,
  from = c("isco", "soc"),
  code_col = NULL,
  path = NULL,
  augment = FALSE
)
}
\arguments{
\item{data}{A data.frame containing either ISCO or SOC codes to map.
If NULL, the raw crosswalk table is returned.}

\item{from}{Character, either "isco" or "soc" (direction of mapping).}

\item{code_col}{Character name of the code column in \code{data} to use. If NULL,
defaults to \code{isco08_code} for ISCO and \code{soc2010} for SOC.}

\item{path}{Optional path to crosswalk Excel file (passed to loader).}

\item{augment}{Logical; if TRUE add \code{mapped_code} and \code{mapped_code_type} to
the returned rows; if FALSE return only mapped rows with original cols plus
mapping columns.}
}
\value{
A data.frame with expanded mappings.
}
\description{
Map a data.frame from ISCO -> SOC or SOC -> ISCO. Many-to-many mappings are
expanded: each input row may produce multiple output rows when multiple
mapped codes exist. By default the function returns a new data.frame with
mapped rows; if \code{augment = TRUE} the function will add \code{mapped_code} and
\code{mapped_code_type} columns to the original rows and return an expanded
data.frame as well.
}
