% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{base_isco_detail}
\alias{base_isco_detail}
\title{Load ISCO-08 detailed structure (all levels with definitions)}
\usage{
base_isco_detail(path = NULL, digits = NULL)
}
\arguments{
\item{path}{optional path to CSV; if NULL uses inst/extdata/source_isco_structure.csv}

\item{digits}{optional integer (1, 2, 3, or 4) to filter to specific level; NULL returns all}
}
\value{
tibble with columns: isco08_code, n_digits, isco08_title, definition, tasks_include, etc.
}
\description{
Returns the full ISCO-08 hierarchical structure including 1-, 2-, 3-, and 4-digit
codes with titles, definitions, task descriptions, and notes.
Use \code{n_digits} column to filter by level (e.g., \code{filter(n_digits == 4)} for 4-digit only).
}
\examples{
# Load all levels
# isco_detail <- base_isco_detail()

# Load only 4-digit codes
# isco_4dig <- base_isco_detail(digits = 4)

# Load 2-digit major groups
# isco_2dig <- base_isco_detail(digits = 2)
}
